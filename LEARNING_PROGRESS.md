# 🎓 План обучения: Автотестировщик → SDET/DevOps

**Студент:** Admin
**Начало обучения:** 2025-12-21
**Длительность:** 12 недель
**Статус:** 🟢 В процессе

---

## 🎯 Текущий прогресс

**Текущая неделя:** Неделя 5 - Docker Fundamentals 🐳
**Текущий этап:** День 19 завершён ✅
**Последнее обновление:** 2026-01-12

**Что сделано (День 19):**
- ✅ Детальный разбор слоёв Docker через `docker history`
- ✅ Понял концепцию слоёв: кэширование, переиспользование, быстрая доставка
- ✅ Docker networking — как контейнеры общаются (docker network ls, inspect)
- ✅ Понял DNS внутри Docker сети (обращение по имени сервиса)
- ✅ Повторил docker-compose.yml (уже изучал ранее)
- ⚠️ VPN конфликт с WSL — пропустили практику `docker-compose build`

**Следующий шаг:** Неделя 6 — Написать свой Dockerfile (multi-stage build)

**Проблема для решения:** VPN ломает сеть WSL → отключать VPN для docker build

---

## 📋 Чек-лист выполненных задач

### ✅ Настройка окружения

- [x] Проверка установленных инструментов (Docker, WSL, Git, PyCharm)
- [x] Настройка WSL Ubuntu
- [x] Добавление алиасов в ~/.bashrc:
  - [x] `alias dc='docker-compose'`
  - [x] `alias dcup='docker-compose up'`
  - [x] `alias dcdown='docker-compose down'`
  - [x] `alias dclogs='docker-compose logs'`
  - [x] `alias dcps='docker-compose ps'`
  - [x] Оставил стандартный `ll` (ls -alF)
- [x] Перезагрузка конфигурации: `source ~/.bashrc`
- [ ] Настройка PyCharm terminal на WSL
- [ ] Настройка Docker в PyCharm
- [ ] Настройка Python Interpreter → WSL в PyCharm

---

## 📅 ФАЗА 1: Основы Linux и работа с логами (Недели 1-3)

### 🗓️ Неделя 1: Linux Essentials + Навигация (Дни 1-7)

**Цель:** Уверенная работа в терминале и понимание логов

#### День 1 (2025-12-21 → 2025-12-22) - Настройка окружения и первый нагрузочный тест ✅

**Задачи:**
- [x] Настройка алиасов в WSL
- [x] Запуск проекта DockerKube в WSL
- [x] Выполнение задачи "Баг в продакшене" (часть 1)
- [x] Создание папки `scripts/` для хранения скриптов (уже была создана)

**Выполненная задача:** Реальная задача №1 - "Баг в продакшене" (Часть 1 - Генерация и автоматический анализ)

Шаги:
- [x] Перейти в проект: `cd /mnt/c/Users/Admin/PycharmProjects/DockerKube`
- [x] Запустить все сервисы: сервисы уже были запущены (backend + postgres)
- [x] Проверить статус: `dcps` (оба контейнера работают)
- [x] Использовать готовый скрипт `load_test.py` для генерации нагрузки
- [x] Запустить нагрузочный тест: `python load_test.py`
- [x] Проанализировать логи автоматически: `./analyze_logs.sh`
- [x] Посчитать статистику ошибок (через analyze_logs.sh)
- [ ] **ЗАВТРА:** Ручной анализ логов (grep, awk, wc, sed)
- [ ] **ЗАВТРА:** Сохранить ошибки в файл вручную
- [ ] **ЗАВТРА:** Глубокий анализ каждого типа ошибки

**Результаты Дня 1:**
- ✅ Сгенерировано 724 HTTP запроса
- ✅ Получена статистика: 82% успешных, 18% ошибок
- ✅ Типы ошибок: 400 (72), 404 (19), 422 (36)
- ✅ Освоил запуск скриптов в WSL
- ✅ Освоил алиасы docker-compose (dc, dcup, dcdown, dclogs, dcps)
- ✅ Изучил команду `source ~/.bashrc` (перезагрузка конфига терминала)
- ✅ Остановил контейнеры перед уходом (`dcdown`)

**Заметки:**
- Алиасы работают только в интерактивном shell, не в скриптах
- Можно использовать как короткие (dc), так и полные команды (docker-compose)
- Оставил стандартный alias ll='ls -alF' вместо ls -lah
- analyze_logs.sh использует grep, awk, sed для парсинга логов
- load_test.py генерирует 30% запросов с ошибками для практики анализа

**Workflow на будущее:**
- В дальнейшем планирую использовать **Вариант 3: Профессиональный подход**
- Это включает: проверку логов, сохранение в файл, проверку ресурсов, git commit
- Сегодня просто остановил контейнеры (поздно, учусь базовому workflow)

---

#### День 2 (2025-12-22) - Ручной анализ логов и практика Linux команд ✅

**Цель:** Научиться анализировать логи вручную с помощью grep, awk, wc, sed

**Задачи:**
- [x] Задание 1: Найти все ошибки 422 с помощью grep
- [x] Задание 2: Сохранить все ошибки в файл errors.log
- [x] Задание 3: Статистика ошибок по типам (grep + awk + sort + uniq)
- [x] Задание 4: Найти конкретные примеры каждой ошибки
- [x] Задание 5: Временной диапазон ошибок (первая и последняя)
- [x] Задание 6: Подсчёт уникальных IP адресов
- [x] Задание 7: Создать отчёт о проблеме (bug_report.txt)
- [x] Практика фильтрации логов (grep, awk, sed, wc)

**Команды для практики:**
```bash
# Задание 1
dclogs backend | grep "422"
dclogs backend | grep "422" | wc -l

# Задание 2
dclogs backend | grep -E "(400|404|422)" > errors.log

# Задание 3
dclogs backend | grep -oE 'HTTP/1\.1" [4-5][0-9]{2}' | awk '{print $2}' | sort | uniq -c

# Задание 4
dclogs backend | grep "422" | head -3
dclogs backend | grep "400" | head -3

# Задание 5
dclogs backend | grep "422" | head -1
dclogs backend | grep "422" | tail -1

# Задание 6
dclogs backend | grep -oE '[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+' | sort | uniq -c
```

**Что изучу:**
- Фильтрация текста с помощью grep и регулярных выражений
- Обработка столбцов с помощью awk
- Подсчёт и сортировка с помощью sort, uniq, wc
- Перенаправление вывода в файлы (>, >>)
- Pipes (|) для связывания команд

---

#### День 3 (2025-12-23) - Создание профессиональных bash скриптов ✅

**Цель:** Создать production-ready скрипты для автоматизации рутинных задач

**Задачи:**
- [x] Организация папки `scripts/` и документации
- [x] Создание `cleanup_logs.sh` - очистка старых логов
- [x] Создание `monitoring_script.sh` - мониторинг здоровья сервисов
- [x] Создание `backup_logs.sh` - бэкап логов с timestamp
- [x] Добавление правил в CLAUDE.md про создание скриптов с best practices
- [x] Документирование всех команд и скриптов

**Созданные скрипты:**

**1. cleanup_logs.sh** - Очистка старых логов
- Поиск и удаление логов старше N дней
- Dry-run режим по умолчанию (безопасность)
- Флаг --force для реального удаления
- Параметры: --days, --pattern, --dir
- Цветной вывод и детальная справка

**2. monitoring_script.sh** - Мониторинг Docker сервисов
- Проверка статуса контейнеров (docker-compose ps)
- Health check PostgreSQL (pg_isready)
- Health check Backend API (curl)
- Мониторинг ресурсов (docker stats)
- Анализ логов на ошибки
- Итоговый отчёт с процентом успешных проверок
- Параметры: --quick, --service, --logs

**3. backup_logs.sh** - Бэкап логов с timestamp
- Сохранение логов с timestamp в имени файла
- Архивация в tar.gz (опционально)
- Автоматическая очистка старых архивов
- Dry-run режим
- Параметры: --service, --archive, --cleanup-days, --lines

**Изученные команды:**
- `date +%Y%m%d_%H%M%S` - форматирование даты для timestamp
- `tar -czf` - создание tar.gz архивов
- `find -mtime +N` - поиск файлов по дате модификации
- `docker-compose logs --no-color --timestamps` - сохранение логов
- `docker inspect --format` - извлечение информации о контейнере
- `docker stats --no-stream` - мониторинг ресурсов
- `curl -w "%{http_code}"` - HTTP проверки
- `command -v` - проверка наличия команды
- `du -ch` - подсчёт размера файлов
- `mkdir -p` - создание вложенных директорий

**Best practices применённые:**
- Shebang и заголовочные комментарии
- Цветной вывод (RED, GREEN, YELLOW, CYAN)
- Функция show_usage() для справки
- Валидация входных параметров
- Обработка ошибок и проверка exit codes
- Dry-run режим для деструктивных операций
- Детальные комментарии к каждой секции
- Счётчики и итоговые отчёты

**Организация проекта:**
- Создана директория `logs/` для хранения логов
- Создана директория `logs/backups/` для архивов
- Обновлён `scripts/README.md` с описанием всех скриптов
- Добавлены правила в `CLAUDE.md` про best practices

---

### 🗓️ Неделя 2: Работа с логами в Docker (Дни 4-10)

**Цель недели:** Углубиться в работу с Docker logs, научиться эффективно фильтровать и анализировать логи контейнеров.

#### День 4 (2025-12-25) - Docker Logs Fundamentals 🔄 В процессе

**Цель:** Понять разницу между docker logs и docker-compose logs, освоить основные флаги.

**Теория (изучено):**
- [x] Разница между `docker logs` и `docker-compose logs`
- [x] Когда использовать каждую команду
- [x] 7 ключевых флагов для работы с логами

**Изученные флаги:**
- [x] `--tail=N` - Показать последние N строк
- [x] `-f / --follow` - Следить за логами в реальном времени
- [x] `-t / --timestamps` - Показать время каждого лога
- [x] `--since=TIME` - Логи с определённого момента
- [x] `--until=TIME` - Логи до определённого момента
- [x] `--no-color` - Без цветового форматирования
- [x] Указание нескольких сервисов одновременно

**Практические задания:**
- [ ] Задание 1: Базовый просмотр логов (tail)
- [ ] Задание 2: Следить за логами в реальном времени (2 терминала)
- [ ] Задание 3: Логи с timestamp
- [ ] Задание 4: Логи за последние N минут (с нагрузочным тестом)
- [ ] Задание 5: Сохранение логов в файл без цветов
- [ ] Задание 6: Временное окно (since + until)
- [ ] Задание 7: Комбинированная команда (все флаги вместе)

**Команды для практики:**
```bash
# Базовые команды
dclogs --tail=20 backend
dclogs -f --tail=10 backend
dclogs -t --tail=5 backend

# Временные фильтры
dclogs --since=30m backend
dclogs --since="2025-12-25T10:00:00" --until="2025-12-25T11:00:00" backend

# Сохранение в файл
dclogs --no-color backend > logs/backend_clean.log

# Комбинированная (мощная команда)
dclogs -f -t --tail=20 --since=5m backend
```

**Таблица флагов (справка):**
| Флаг | Короткая форма | Что делает | Пример |
|------|----------------|------------|--------|
| `--tail=N` | - | Последние N строк | `--tail=20` |
| `--follow` | `-f` | Реальное время | `-f` |
| `--timestamps` | `-t` | Показать время | `-t` |
| `--since=TIME` | - | Логи с момента | `--since=30m` |
| `--until=TIME` | - | Логи до момента | `--until="10:00:00"` |
| `--no-color` | - | Без цветов | `--no-color` |

---

#### День 5 (2025-12-25) - Фильтрация и анализ логов ✅

**Цель:** Научиться комбинировать docker-compose logs с Linux утилитами для глубокого анализа.

**Выполненные задачи:**
- [x] Запустить тесты и найти все FAILED тесты в логах
- [x] Сохранить логи только PostgreSQL за последние 100 строк
- [x] Найти все HTTP запросы с кодом 4xx или 5xx
- [x] Комбинирование docker-compose logs с grep, awk, sed
- [x] Статистика по HTTP кодам
- [x] Временной анализ ошибок
- [x] Создание структурированных отчётов

**Освоенные техники:**
```bash
# Комбинации с grep/awk/sed
dclogs backend | grep -oE 'HTTP/1\.1" [0-9]{3}' | awk '{print $2}' | sort | uniq -c | sort -rn

# Поиск ошибок с контекстом
dclogs backend | grep -C 3 -E "(400|404|422|500)"

# Создание отчётов
dclogs --no-color backend | grep -E "(ERROR|4[0-9]{2}|5[0-9]{2})" > logs/errors_only.log
```

---

#### День 6 (2025-12-27) - Структура логов и документация ✅ (частично)

**Цель:** Организовать структуру логов и создать документацию по скриптам автоматизации.

**Выполненные задачи:**
- [x] Создана чёткая структура папки `logs/`
  - [x] `logs/raw/` - ручные сохранения логов (по сервисам)
  - [x] `logs/backups/` - ручные backup перед деплоем
  - [x] `logs/rotated/` - автоматическая ротация (cron)
  - [x] `logs/archives/` - долгосрочное хранение (tar.gz)
  - [x] `logs/analysis/` - результаты анализа (error_reports, slow_requests, stats)
  - [x] `logs/alerts/` - алерты от мониторинга
- [x] Созданы README.md в каждой папке с объяснениями
- [x] Создан `SCRIPTS_GUIDE.md` - шпаргалка "Какой скрипт когда использовать"
  - [x] 5 реальных сценариев использования
  - [x] Примеры команд для всех скриптов
  - [x] Ответы на частые вопросы
- [x] Обновлён `CLAUDE.md` с информацией о структуре логов и скриптах
- [x] Настроен `.gitignore` для сохранения структуры папок в git

**Доступные скрипты автоматизации:**
- ✅ `scripts/find_slow_requests.sh` - Поиск медленных HTTP запросов
- ✅ `scripts/logs_rotation.sh` - Автоматическое сохранение и ротация логов
- ✅ `scripts/live_monitor.sh` - Мониторинг логов в реальном времени с алертами
- ✅ `scripts/error_patterns.sh` - Анализ паттернов ошибок
- ✅ `scripts/backup_logs.sh` - Backup логов перед деплоем
- ✅ `scripts/cleanup_logs.sh` - Очистка старых логов
- ✅ `scripts/monitoring_script.sh` - Проверка здоровья сервисов
- ✅ `scripts/load_test.py` - Генератор нагрузки

**Понял важную концепцию:**
- **Зачем backup логов перед деплоем:**
  - Сравнение "до/после" для выявления регрессий
  - Базовые метрики (error rate, response times)
  - Быстрое расследование инцидентов
  - Compliance и аудит
  - Обоснование отката

**Следующие задачи:**
- [ ] **ПРИОРИТЕТ:** Настроить cron для ежедневных тренировок со скриптами
- [ ] Разобрать оставшиеся скрипты (`live_monitor.sh`, `error_patterns.sh`)
- [ ] Попрактиковаться с каждым скриптом вручную

---

### 🗓️ Неделя 3: Cron и ежедневные тренировки (Дни 7-13)

**🎯 Цель недели:** Настроить автоматизацию через cron и использовать скрипты каждый день для тренировки.

#### День 7 - Знакомство с cron ✅

**Теория (30 минут):**
- [x] Что такое cron и crontab
- [x] Синтаксис cron выражений (минуты часы день месяц день_недели)
- [x] Примеры cron выражений:
  ```
  0 2 * * *     - каждый день в 2:00
  */30 * * * *  - каждые 30 минут
  0 9 * * 1-5   - каждый будний день в 9:00
  0 0 * * 0     - каждое воскресенье в полночь
  ```
- [x] Команды: `crontab -l`, `crontab -e`, `crontab -r`

**Практика:**
- [x] Открыть crontab: `crontab -e`
- [x] Создать первую задачу: логирование в файл каждую минуту
  ```bash
  * * * * * echo "$(date): Cron работает!" >> /tmp/cron_test.log
  ```
- [x] Проверить что задача работает: `tail -f /tmp/cron_test.log`
- [x] Удалить тестовую задачу

**Полезные ресурсы:**
- [x] Сайт для генерации cron выражений: https://crontab.guru/

**Результаты Дня 7:**
- ✅ Изучил синтаксис cron выражений
- ✅ Научился работать с crontab (создание, просмотр, редактирование)
- ✅ Протестировал первую задачу в cron
- ✅ Понял как работает автоматизация через cron

---

#### День 8 - Настройка ежедневной ротации логов ✅

**Цель:** Настроить автоматическую ротацию логов каждую ночь.

**Задачи:**
- [x] Добавить в crontab ежедневную ротацию в 2:00
  ```bash
  0 2 * * * cd /path/to/DockerKube && ./scripts/logs_rotation.sh --service=all --compress --max-files=7
  ```
- [x] Добавить логирование выполнения cron задачи
  ```bash
  0 2 * * * cd /path/to/DockerKube && ./scripts/logs_rotation.sh --service=all --compress --max-files=7 >> logs/cron.log 2>&1
  ```
- [x] Проверить что задача добавлена: `crontab -l`
- [x] Протестировать скрипт вручную перед автоматизацией

**Что проверить утром:**
- [ ] Посмотреть `logs/rotated/` - появились новые .gz файлы?
- [ ] Посмотреть `logs/cron.log` - есть ли вывод скрипта?
- [ ] Размер файлов - правильно сжаты?

**Результаты Дня 8:**
- ✅ Настроена ежедневная ротация логов через cron (каждый день в 2:00)
- ✅ Добавлено логирование выполнения cron задачи в `logs/cron.log`
- ✅ Протестирован скрипт `logs_rotation.sh` вручную
- ✅ Понял как автоматизировать рутинные задачи обслуживания

**Следующий шаг:** На следующее утро проверить что ротация отработала автоматически

---

#### День 9 - Ежедневные отчёты об ошибках ✅

**Цель:** Получать HTML отчёты об ошибках каждое утро.

**Задачи:**
- [x] Протестировать скрипт `error_patterns.sh` вручную
- [x] Сгенерировать HTML отчёт и открыть в браузере
- [x] Добавить генерацию отчёта в cron на 3:00 (после ротации)
  ```bash
  0 3 * * * cd /mnt/c/Users/Admin/PycharmProjects/DockerKube && ./scripts/error_patterns.sh --service=backend --report --output=logs/analysis/error_reports/backend_errors_$(date +\%Y\%m\%d).html >> logs/cron.log 2>&1
  ```
- [ ] Проверить на следующий день:
  - [ ] Появился файл в `logs/analysis/error_reports/`?
  - [ ] Открыть HTML отчёт в браузере

**Изученные концепции:**
- Разница между `>` (перезапись) и `>>` (добавление)
- `2>&1` - перенаправление stderr в stdout
- Экранирование `%` как `\%` в crontab
- Открытие WSL файлов в Windows: `explorer.exe путь/`

**Утренний ритуал (начиная с этого дня):**
1. Открыть терминал
2. Посмотреть последний отчёт об ошибках
3. Если есть проблемы - разобраться
4. Если нет - продолжить обучение

**Результаты Дня 9:**
- ✅ Скрипт error_patterns.sh протестирован
- ✅ HTML отчёт генерируется корректно
- ✅ Cron задача настроена на 3:00
- ✅ Понял работу перенаправления вывода (>>, 2>&1)

---

#### День 10 - Еженедельная очистка старых логов ✅

**Цель:** Настроить автоматическую очистку логов каждый понедельник.

**Задачи:**
- [x] Протестировать `cleanup_logs.sh` в dry-run режиме
- [x] Понять синтаксис дня недели в cron (0=Вс, 1=Пн, 2=Вт...)
- [x] Добавить backup каждое воскресенье в 23:00
  ```bash
  0 23 * * 0 cd /mnt/c/Users/Admin/PycharmProjects/DockerKube && ./scripts/backup_logs.sh --service=all --archive >> logs/cron.log 2>&1
  ```
- [x] Добавить очистку каждый понедельник в 5:00
  ```bash
  0 5 * * 1 cd /mnt/c/Users/Admin/PycharmProjects/DockerKube && ./scripts/cleanup_logs.sh --days=30 --force >> logs/cron.log 2>&1
  ```

**Изученные концепции:**
- День недели в cron: 0=Вс, 1=Пн, 2=Вт, 3=Ср, 4=Чт, 5=Пт, 6=Сб
- Cron НЕ выполняет пропущенные задачи (если ПК был выключен)
- Альтернативы: anacron, ручной запуск, проверка в ~/.bashrc
- Логика: сначала backup (Вс) → потом cleanup (Пн)

**Результаты Дня 10:**
- ✅ Скрипт cleanup_logs.sh протестирован (нашёл 8 старых файлов)
- ✅ Backup настроен на воскресенье 23:00
- ✅ Cleanup настроен на понедельник 5:00
- ✅ Понял поведение cron при недоступности системы

---

#### День 11-13 - Практика с остальными скриптами ✅

**Цель:** Использовать все скрипты в разных комбинациях для тренировки.

**День 11 - live_monitor.sh и find_slow_requests.sh:**
- [x] Практика с `live_monitor.sh` — мониторинг в реальном времени
- [x] Запуск в двух терминалах (мониторинг + нагрузка)
- [x] Практика с `find_slow_requests.sh` — поиск медленных запросов
- [x] Понял что скрипт требует логирования response time в backend

**День 12 - monitoring_script.sh:**
- [x] Полная проверка здоровья сервисов
- [x] Быстрая проверка (`--quick`)

**День 13 - Реальные сценарии:**
- [x] Сценарий 1: Утренняя проверка (Daily Routine)
- [x] Сценарий 2: Перед деплоем (Pre-Deploy)
- [x] Сценарий 3: Расследование инцидента (Incident Response)
- [x] Практика расследования: анализ 8 ошибок за 8 часов

**Изученные скрипты (полный список):**
| Скрипт | Назначение |
|--------|------------|
| `monitoring_script.sh` | Health check всех сервисов |
| `live_monitor.sh` | Мониторинг логов в реальном времени |
| `find_slow_requests.sh` | Поиск медленных HTTP запросов |
| `error_patterns.sh` | Анализ паттернов ошибок + HTML отчёты |
| `backup_logs.sh` | Backup логов перед деплоем |
| `logs_rotation.sh` | Ежедневная ротация логов |
| `cleanup_logs.sh` | Очистка старых логов |

**Результаты Дней 11-13:**
- ✅ Освоил все 7 скриптов автоматизации
- ✅ Понял когда какой скрипт применять
- ✅ Провёл расследование инцидента по реальному сценарию

---

#### 📊 Итоговая конфигурация cron (к концу недели 3)

```bash
# Посмотреть текущую конфигурацию
crontab -l

# Должно быть:
# Ежедневная ротация логов в 2:00
0 2 * * * cd /path/to/DockerKube && ./scripts/logs_rotation.sh --service=all --compress --max-files=7 >> logs/cron.log 2>&1

# Ежедневный отчёт об ошибках в 3:00
0 3 * * * cd /path/to/DockerKube && ./scripts/error_patterns.sh --service=backend --report >> logs/cron.log 2>&1

# Утренняя проверка здоровья в 9:00
0 9 * * * cd /path/to/DockerKube && ./scripts/monitoring_script.sh --quick >> logs/morning_check.log 2>&1

# Воскресенье 23:00 - архивация
0 23 * * 0 cd /path/to/DockerKube && ./scripts/backup_logs.sh --service=all --archive >> logs/cron.log 2>&1

# Понедельник 5:00 - очистка старых логов
0 5 * * 1 cd /path/to/DockerKube && ./scripts/cleanup_logs.sh --days=30 --force >> logs/cron.log 2>&1

# Каждые 6 часов - поиск медленных запросов
0 */6 * * * cd /path/to/DockerKube && ./scripts/find_slow_requests.sh --threshold=1.0 >> logs/slow_requests_auto.log 2>&1
```

**Результат:** Полностью автоматизированный цикл обслуживания логов + ежедневная тренировка!

---

### 🗓️ Неделя 4: jq, awk, sed - продвинутый анализ (Дни 14-20)

**🎯 Цель недели:** Освоить продвинутые инструменты анализа текста и JSON.

#### День 14 (2026-01-10) - awk: базовый синтаксис и колонки ✅

**Изученные техники:**
- [x] Извлечение колонок: `awk '{print $4, $9}'`
- [x] Фильтрация по условию: `awk '$9 >= 400'`
- [x] Подсчёт в массивах: `awk '{arr[$7]++} END {for...}'`
- [x] Разделитель -F: `awk -F':' '{print $1}'`
- [x] Встроенные функции: `length()`, `NR`, `NF`

**Практические задания:**
- [x] Извлечение IP-адресов из логов
- [x] Извлечение HTTP-кодов
- [x] Фильтрация ошибок (код >= 400)
- [x] Статистика по HTTP-кодам
- [x] Статистика по endpoints
- [x] Подсчёт уникальных IP-адресов

---

#### День 15 (2026-01-10) - sed: поиск и замена ✅

**Изученные техники:**
- [x] Простая замена: `sed 's/old/new/'`
- [x] Глобальная замена: `sed 's/old/new/g'`
- [x] Удаление строк: `sed '/pattern/d'`
- [x] Множественные замены: `sed -e 's/a/b/' -e 's/c/d/'`
- [x] Начало/конец строки: `^` и `$`

**Практические задания:**
- [x] Замена текста в логах
- [x] Удаление префикса контейнера
- [x] Фильтрация строк по паттерну
- [x] Добавление префиксов/суффиксов
- [x] Очистка логов для анализа

---

#### День 16 (2026-01-10) - jq: парсинг JSON ✅

**Изученные техники:**
- [x] Извлечение поля: `jq '.name'`
- [x] Raw output: `jq -r '.field'`
- [x] Элементы массива: `jq '.[]'` или `jq '.[0]'`
- [x] Фильтрация: `jq 'select(.status == "running")'`
- [x] Форматирование: `jq '"\(.id) \(.name)"'`

**Практические задания:**
- [x] Установка jq (скачан бинарник вручную)
- [x] docker inspect + jq для извлечения данных контейнера
- [x] Извлечение переменных окружения
- [x] Работа с API ответами
- [x] Подсчёт и фильтрация контейнеров
- [x] Создание отчётов в формате JSON

---

#### День 17 (2026-01-10 → 2026-01-11) - Комбинации инструментов ✅

**Изученные техники:**
- [x] Полный отчёт о системе (docker + jq + grep + awk)
- [x] Топ-10 частых ошибок (grep + sed + awk + sort + uniq)
- [x] Мониторинг ресурсов + логи
- [x] Анализ ошибок по времени

**Финальное задание ВЫПОЛНЕНО:**
- [x] Создан скрипт `scripts/log_stats.sh` — комплексный анализ логов
  - Статистика HTTP кодов (awk)
  - Топ ошибок с примерами (grep + awk + sed)
  - Топ IP-адресов (awk)
  - Топ эндпоинтов (awk + sed)
  - Анализ Docker контейнеров (docker inspect + jq)
  - JSON вывод для автоматизации
  - Итоговая сводка с рекомендациями

---

**Теория (изучено):**
- [x] jq для парсинга JSON логов
- [x] awk для колоночных данных
- [x] sed для поиска и замены
- [x] Комбинации инструментов

**Практика:**
- [x] Парсинг JSON (docker inspect, API)
- [x] AWK магия для анализа
- [x] Подсчёт статистики

**Задания:**
- [x] Создать скрипт для сбора статистики (log_stats.sh) ✅ ЗАВЕРШЕНО
- [x] Написать команду для поиска 10 самых частых ошибок
- [x] Научиться использовать docker inspect + jq

---

## 📅 ФАЗА 2: Docker глубокое погружение (Недели 4-6)

### 🗓️ Неделя 4: Docker Fundamentals (Дни 22-28)

**Теория (4-5 часов):**
- [ ] Образы vs Контейнеры
- [ ] Слои Docker (layers) и кэширование
- [ ] Docker registry

**Задания:**
- [ ] Пересобрать backend образ без кэша
- [ ] Изучить размер каждого слоя образа backend
- [ ] Зайти в PostgreSQL контейнер и выполнить SQL запросы
- [ ] Создать свой Dockerfile для простого Python скрипта

---

### 🗓️ Неделя 5: Dockerfile - пишем свои образы (Дни 29-35)

**Задания:**
- [ ] Перепиши backend.Dockerfile с multi-stage build
- [ ] Создай Dockerfile для утилиты мониторинга логов
- [ ] Уменьши размер образа backend минимум на 30%
- [ ] Создай .dockerignore файл

---

### 🗓️ Неделя 6: Docker Compose - оркестрация (Дни 36-42)

**Задания:**
- [ ] Изучить docs/learning/docker-compose-explained.md
- [ ] Добавить Redis в docker-compose.yml
- [ ] Настроить resource limits для всех контейнеров
- [ ] Создать docker-compose.override.yml для dev-окружения

---

## 📅 ФАЗА 3: CI/CD и автоматизация (Недели 7-9)

### 🗓️ Неделя 7: Git + GitLab/GitHub CI (Дни 43-49)

**Задания:**
- [ ] Создать GitHub Actions workflow для автоматического запуска тестов
- [ ] Настроить автоматическую сборку Docker образов при push
- [ ] Добавить badge со статусом тестов в README
- [ ] Настроить уведомления о падении тестов

---

### 🗓️ Неделя 8: Kubernetes основы (Дни 50-56)

**Задания:**
- [ ] Установить Minikube
- [ ] Развернуть весь проект в Minikube
- [ ] Настроить автоматический сбор логов из всех подов
- [ ] Добавить Ingress для доступа к API
- [ ] Создать ConfigMap и Secret

---

### 🗓️ Неделя 9: Мониторинг и логирование (Дни 57-63)

**Задания:**
- [ ] Настроить Loki для сбора логов Docker
- [ ] Создать Grafana dashboard с метриками API
- [ ] Настроить алерты на ошибки в логах
- [ ] Интегрировать логи с pytest тестами

---

## 📅 ФАЗА 4: Advanced DevOps практики (Недели 10-12)

### 🗓️ Неделя 10: Infrastructure as Code (Дни 64-70)

**Задания:**
- [ ] Перепиши docker-compose.yml в Terraform
- [ ] Создай Ansible playbook для деплоя проекта
- [ ] Создай Helm chart для K8s deployment

---

### 🗓️ Неделя 11: Security и Best Practices (Дни 71-77)

**Задания:**
- [ ] Настроить автоматическое сканирование образов в CI
- [ ] Исправить все найденные уязвимости
- [ ] Перепиши Dockerfiles с непривилегированным пользователем
- [ ] Интегрировать Vault для секретов

---

### 🗓️ Неделя 12: Финальный проект (Дни 78-84)

**Задачи:**
- [ ] Создать полноценный CI/CD pipeline
- [ ] Деплой в Kubernetes с мониторингом
- [ ] Автоматические тесты + сбор логов
- [ ] Dashboard с метриками и логами
- [ ] Документация

---

## 📝 Созданные скрипты и файлы

### Скрипты для автоматизации:

- [ ] `scripts/load_test.py` - Генератор нагрузки для тестирования API
- [ ] `scripts/monitoring_script.sh` - Мониторинг здоровья Docker сервисов
- [ ] `scripts/cleanup_logs.sh` - Очистка старых логов
- [ ] `scripts/test_report.sh` - Автоматический отчёт о тестах
- [ ] `scripts/backup_logs.sh` - Backup логов с timestamp

---

## 🎯 Контрольные точки

### ✅ Через 1 месяц (конец Недели 4)

- [ ] Уверенно работать в Linux терминале
- [ ] Читать и анализировать Docker логи
- [ ] Понимать каждую строку в docker-compose.yml

### ✅ Через 2 месяца (конец Недели 8)

- [ ] Писать свои Dockerfiles
- [ ] Настраивать CI/CD pipeline
- [ ] Деплоить в Kubernetes

### ✅ Через 3 месяца (конец Недели 12)

- [ ] Настроить полный цикл: build → test → deploy → monitor
- [ ] Решать production инциденты через логи
- [ ] Готов к интервью на SDET/DevOps позицию

---

## 📚 Изученные материалы

### Документация:
- [ ] Docker Documentation (docs.docker.com)
- [x] docs/learning/docker-compose-explained.md
- [ ] Kubernetes Documentation (kubernetes.io)

### Книги:
- [ ] "The Linux Command Line" - William Shotts
- [ ] "The Phoenix Project"
- [ ] "Site Reliability Engineering" - Google

### Курсы:
- [ ] Linux Journey (linuxjourney.com)
- [ ] OverTheWire: Bandit
- [ ] Docker Mastery - Bret Fisher (Udemy)
- [ ] Certified Kubernetes Administrator (CKA)

---

## 💡 Заметки и важные моменты

### Настройка окружения:
- WSL: Ubuntu (VERSION 2)
- Docker Desktop установлен и работает
- Git настроен
- PyCharm готов к работе

### Полезные команды для быстрого старта:

```bash
# Перейти в проект
cd /mnt/c/Users/Admin/PycharmProjects/DockerKube

# Запустить всё
dcup -d

# Проверить статус
dcps

# Посмотреть логи
dclogs backend

# Остановить всё
dcdown

# Запустить мониторинг
./scripts/monitoring_script.sh
```

### Вопросы для изучения:
- (пусто - добавляй сюда свои вопросы в процессе обучения)

### Проблемы и решения:
- (пусто - документируй проблемы и как их решил)

---

## 🔄 Следующие шаги

**На следующую сессию (Неделя 3, День 7 - Знакомство с cron):**

### ✅ Завершено в День 6 (2025-12-27):
- ✅ Создана организованная структура папки `logs/` (raw, backups, rotated, archives, analysis, alerts)
- ✅ Написаны README.md в каждой папке с подробными объяснениями
- ✅ Создан `SCRIPTS_GUIDE.md` - полная шпаргалка по скриптам (5 сценариев)
- ✅ Обновлён `CLAUDE.md` с информацией о структуре логов и автоматизации
- ✅ Понял концепцию: зачем backup логов перед деплоем (сравнение до/после, базовые метрики)
- ✅ Настроен `.gitignore` для сохранения структуры папок в git

### 📝 План на Неделю 3: Cron и ежедневные тренировки

**🎯 Главная цель:** Настроить автоматизацию через cron и начать использовать скрипты каждый день для реальной практики.

**День 7 - Знакомство с cron (следующий):**

1. **Изучить теорию (30 минут):**
   - Что такое cron и crontab
   - Синтаксис cron выражений
   - Команды: `crontab -l`, `crontab -e`, `crontab -r`

2. **Первая практика:**
   ```bash
   # Открыть crontab
   crontab -e

   # Добавить тестовую задачу (каждую минуту)
   * * * * * echo "$(date): Cron работает!" >> /tmp/cron_test.log

   # Проверить что работает
   tail -f /tmp/cron_test.log

   # Удалить тестовую задачу
   ```

3. **Ресурсы:**
   - 🌐 https://crontab.guru/ - генератор cron выражений

**Дальше:**
- День 8: Настроить ежедневную ротацию логов в 2:00
- День 9: Настроить ежедневные отчёты об ошибках в 3:00
- День 10: Еженедельная очистка каждый понедельник
- Дни 11-13: Практика со всеми скриптами

**Итоговая конфигурация к концу недели:**
```bash
# Ночная ротация логов
0 2 * * * cd /path/to/DockerKube && ./scripts/logs_rotation.sh --service=all --compress --max-files=7

# Отчёты об ошибках
0 3 * * * cd /path/to/DockerKube && ./scripts/error_patterns.sh --service=backend --report

# Утренняя проверка здоровья
0 9 * * * cd /path/to/DockerKube && ./scripts/monitoring_script.sh --quick

# Воскресенье - архивация
0 23 * * 0 cd /path/to/DockerKube && ./scripts/backup_logs.sh --service=all --archive

# Понедельник - очистка
0 5 * * 1 cd /path/to/DockerKube && ./scripts/cleanup_logs.sh --days=30 --force
```

**Команды для быстрого старта:**

1️⃣ **Открыть WSL терминал:**
   ```bash
   wsl
   # или через Windows Terminal → Ubuntu
   ```

2️⃣ **В WSL перейти в проект:**
   ```bash
# Перейти в проект
cd /mnt/c/Users/Admin/PycharmProjects/DockerKube

# Перезагрузить алиасы (если нужно)
source ~/.bashrc

# Поднять контейнеры
dcup -d postgres backend

# Проверить статус (должны быть Running)
dcps

# Проверить что API работает
curl http://localhost:8000/
```

---

## ✅ Daily Workflow (как в реальной работе)

### 🌅 Начало рабочего дня:

```bash
# В WSL
cd /mnt/c/Users/Admin/PycharmProjects/DockerKube

# Проверь статус
dcps

# Если контейнеры остановлены, запусти
dcup -d postgres backend

# Проверь логи (что всё запустилось корректно)
dclogs backend --tail=50

# Проверь что API работает
curl http://localhost:8000/

# Начинай практику
```

---

### 🌆 Конец рабочего дня (перед уходом):

**Вариант 1: Оставить контейнеры запущенными (для долгих тестов или непрерывной работы)**
```bash
# Проверить что всё работает
dcps

# Посмотреть использование ресурсов
docker stats --no-stream

# Можно оставить запущенным - контейнеры легковесны
# Это нормально для dev-окружения
```

✅ **Плюсы:** Завтра сразу продолжишь работу, не ждёшь запуска
⚠️ **Минусы:** Занимают RAM (~500MB для этого проекта)

---

**Вариант 2: Остановить контейнеры (рекомендуется для обучения)**
```bash
# Остановить все контейнеры
dcdown

# Проверить что всё остановлено
docker ps

# Если хочешь ПОЛНОСТЬЮ очистить (удалить данные БД)
dcdown -v  # ВНИМАНИЕ: удалит все данные в PostgreSQL!
```

✅ **Плюсы:** Освобождает RAM, чистое окружение завтра
⚠️ **Минусы:** Завтра нужно ждать ~10-15 секунд пока запустятся

---

**Вариант 3: Профессиональный подход (как в реальной работе)**
```bash
# 1. Проверить статус
dcps

# 2. Посмотреть логи на наличие ошибок
dclogs backend --tail=100 | grep -i error

# 3. Сохранить важные логи (если нужно)
dclogs backend > logs/backend_$(date +%Y%m%d).log

# 4. Проверить использование ресурсов
docker stats --no-stream

# 5. Решить: остановить или оставить
# Если тесты/сервисы не нужны ночью - остановить:
dcdown

# 6. Очистить временные файлы (опционально)
rm -f errors.log bug_report.txt

# 7. Сохранить прогресс в Git (если работаешь с репозиторием)
# git add .
# git commit -m "Завершён День 1: нагрузочное тестирование"
# git push
```

---

### 📋 Чек-лист перед уходом:

- [ ] Проверил логи на наличие критических ошибок
- [ ] Остановил контейнеры (если не нужны ночью): `dcdown`
- [ ] Сохранил важные результаты/логи (если есть)
- [ ] Обновил LEARNING_PROGRESS.md (уже сделано автоматически)
- [ ] Закоммитил изменения в Git (опционально для обучения)
- [ ] Выключил ненужные сервисы (Docker Desktop можно оставить)

---

### 💡 Рекомендация для вашего обучения:

**Каждый вечер:**
1. Остановить контейнеры: `dcdown` (без `-v`)
2. Оставить Docker Desktop запущенным
3. Завтра просто запустить: `dcup -d postgres backend`

**Это освободит ~500MB RAM, но сохранит данные в БД.**

---

**Последнее обновление:** 2026-01-09
**Обновил:** Claude Code
**Статус:** Неделя 3, День 8 завершён ✅ | Настроена автоматическая ротация логов
**Следующий шаг:** День 9 - Настройка ежедневных отчётов об ошибках (error_patterns.sh в cron)
