# 📚 Шпаргалка по командам Linux и Docker

**Автор:** Admin
**Дата создания:** 2025-12-22
**Обновлено:** День 4, Неделя 2 (2025-12-25)

> Эта шпаргалка содержит все команды, изученные в рамках обучения SDET/DevOps. Каждая команда проверена на практике в проекте DockerKube.

---

## 🐳 Docker Compose

### Базовые команды

```bash
# Запустить все сервисы
docker-compose up

# Запустить в фоновом режиме (detached)
docker-compose up -d

# Запустить с пересборкой образов
docker-compose up --build

# Остановить все сервисы
docker-compose down

# Остановить и удалить volumes (очистка БД)
docker-compose down -v

# Посмотреть логи всех сервисов
docker-compose logs

# Посмотреть логи конкретного сервиса
docker-compose logs backend

# Следить за логами в реальном времени (-f = follow)
docker-compose logs -f backend

# Посмотреть последние N строк логов
docker-compose logs backend --tail=50

# Посмотреть статус всех сервисов
docker-compose ps

# Запустить разовую команду в сервисе
docker-compose run tests pytest -v
```

### Docker Logs - Детальное руководство (Неделя 2, День 4)

**Разница docker logs vs docker-compose logs:**
- `docker logs` - для одного контейнера
- `docker-compose logs` - для сервисов из docker-compose.yml

**7 ключевых флагов:**

```bash
# 1. --tail=N - Показать последние N строк
dclogs --tail=20 backend          # Последние 20 строк
dclogs --tail=100 postgres        # Последние 100 строк

# 2. -f / --follow - Следить в реальном времени
dclogs -f backend                 # Реальное время (Ctrl+C для выхода)
dclogs --follow backend           # То же самое (длинная форма)

# 3. -t / --timestamps - Показать время
dclogs -t backend                 # С временными метками
dclogs --timestamps backend       # То же самое (длинная форма)

# 4. --since=TIME - Логи с определённого момента
dclogs --since=5m backend         # Последние 5 минут
dclogs --since=30m backend        # Последние 30 минут
dclogs --since=2h backend         # Последние 2 часа
dclogs --since=1d backend         # Последний день
dclogs --since="2025-12-25T10:00:00" backend  # С конкретного времени

# 5. --until=TIME - Логи до определённого момента
dclogs --until="2025-12-25T11:00:00" backend  # До 11:00

# 6. --no-color - Без цветового форматирования
dclogs --no-color backend > logs/backend.log  # Для сохранения в файл

# 7. Несколько сервисов одновременно
dclogs backend postgres           # Логи обоих сервисов
dclogs -f backend postgres        # В реальном времени
```

**Комбинации флагов (самое мощное!):**

```bash
# Последние 20 строк в реальном времени
dclogs -f --tail=20 backend

# С временными метками + последние 10 строк
dclogs -t --tail=10 backend

# Последние 30 минут в реальном времени
dclogs -f --since=30m backend

# Всё вместе: реальное время + timestamps + последние 20 строк + за 5 минут
dclogs -f -t --tail=20 --since=5m backend

# Временное окно (с 10:00 до 11:00)
dclogs --since="2025-12-25T10:00:00" --until="2025-12-25T11:00:00" backend

# Сохранение логов в файл БЕЗ цветов
dclogs --no-color --since=1h backend > logs/backend_$(date +%Y%m%d_%H%M%S).log
```

**Таблица всех флагов:**

| Флаг | Короткая форма | Описание | Пример |
|------|----------------|----------|--------|
| `--tail=N` | - | Последние N строк | `--tail=20` |
| `--follow` | `-f` | Реальное время | `-f` |
| `--timestamps` | `-t` | Добавить время | `-t` |
| `--since=TIME` | - | С момента времени | `--since=30m` |
| `--until=TIME` | - | До момента времени | `--until="11:00:00"` |
| `--no-color` | - | Без ANSI цветов | `--no-color` |

**Когда использовать:**
- `--tail` - Быстрая проверка (перед началом работы)
- `-f` - Отладка в реальном времени (тесты, деплой)
- `-t` - Расследование инцидентов (нужно точное время)
- `--since` - "Что было за последний час?"
- `--since + --until` - Временное окно для анализа
- `--no-color` - Сохранение логов в файл

### Полезные алиасы (WSL)

```bash
# Добавить в ~/.bashrc
alias dc='docker-compose'
alias dcup='docker-compose up'
alias dcdown='docker-compose down'
alias dclogs='docker-compose logs'
alias dcps='docker-compose ps'

# Использование
dc up -d
dclogs backend
dcps
```

---

## 🔍 Анализ логов (grep, sort, uniq)

### grep - Поиск по шаблону

```bash
# Найти все ошибки ERROR
dclogs backend | grep ERROR

# Найти HTTP код 422 (регистронезависимый поиск)
dclogs backend | grep -i "422"

# Найти несколько кодов (расширенный regex)
dclogs backend | grep -E "(422|400|404)"

# Извлечь только HTTP коды (-o = only matching, -E = extended regex)
dclogs backend | grep -oE 'HTTP/1\.1" [0-9]{3}'

# Извлечь IP адреса
dclogs backend | grep -oE '[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+

# Контекст вокруг совпадения (-A after, -B before, -C context)
dclogs backend | grep -A 3 -B 1 ERROR    # 3 строки после, 1 до
dclogs backend | grep -C 2 ERROR          # 2 строки до и после

# Инвертировать поиск (показать строки БЕЗ шаблона)
dclogs backend | grep -v "200"

# Подсчитать количество совпадений
dclogs backend | grep "422" | wc -l
```

**Полезные флаги grep:**
- `-i` - игнорировать регистр (case-insensitive)
- `-E` - расширенный regex (extended regex)
- `-o` - показать только совпадающую часть (only matching)
- `-v` - инвертировать (показать НЕ совпадающие строки)
- `-A N` - показать N строк после совпадения
- `-B N` - показать N строк до совпадения
- `-C N` - показать N строк до и после

### sort - Сортировка данных

```bash
# Сортировка по алфавиту
cat file.txt | sort

# Сортировка в обратном порядке
cat file.txt | sort -r

# Числовая сортировка (-n = numeric)
cat numbers.txt | sort -n

# Числовая сортировка в обратном порядке
cat numbers.txt | sort -rn

# Сортировка и удаление дубликатов
cat file.txt | sort -u
```

**Полезные флаги sort:**
- `-r` - reverse (обратный порядок)
- `-n` - numeric (числовая сортировка)
- `-u` - unique (удалить дубликаты)
- `-k N` - сортировать по N-ой колонке

### uniq - Работа с дубликатами

```bash
# Удалить дубликаты (требует предварительной сортировки!)
cat file.txt | sort | uniq

# Подсчитать количество повторений (-c = count)
cat file.txt | sort | uniq -c

# Показать только дубликаты
cat file.txt | sort | uniq -d

# Показать только уникальные строки (без дубликатов)
cat file.txt | sort | uniq -u
```

**⚠️ ВАЖНО:** `uniq` работает только с отсортированными данными!

### Комбинированные примеры (pipeline)

```bash
# Топ-5 HTTP кодов по частоте
dclogs backend | grep -oE 'HTTP/1\.1" [0-9]{3}' | awk '{print $2}' | sort | uniq -c | sort -rn | head -5

# Топ-5 IP адресов по количеству запросов
dclogs backend | grep -oE '[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+' | sort | uniq -c | sort -rn | head -5

# Количество уникальных IP
dclogs backend | grep -oE '[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+' | sort -u | wc -l

# Все ошибки 4xx и 5xx
dclogs backend | grep -E 'HTTP/1\.1" [4-5][0-9]{2}'

# Статистика по всем кодам ответа
dclogs backend | grep -oE 'HTTP/1\.1" [0-9]{3}' | awk '{print $2}' | sort | uniq -c
```

---

## 📝 Работа с файлами

### Просмотр файлов

```bash
# Показать содержимое файла
cat file.txt

# Показать с нумерацией строк
cat -n file.txt

# Показать первые 10 строк
head file.txt

# Показать первые N строк
head -20 file.txt

# Показать последние 10 строк
tail file.txt

# Показать последние N строк
tail -50 file.txt

# Следить за изменениями в реальном времени
tail -f file.txt

# Просмотр с пагинацией (стрелки вверх/вниз, q = выход)
less file.txt

# Постраничный просмотр (пробел = следующая страница)
more file.txt
```

### Редирект и создание файлов

```bash
# Перезаписать файл (> = overwrite)
echo "Hello" > file.txt

# Добавить в конец файла (>> = append)
echo "World" >> file.txt

# Сохранить вывод команды в файл
dclogs backend > logs.txt

# Добавить вывод команды в конец файла
dclogs backend >> logs.txt

# Сохранить и stderr, и stdout (2>&1)
dclogs backend > logs.txt 2>&1
```

### Редактирование файлов

```bash
# Открыть файл в nano (простой редактор)
nano file.txt

# Сохранить в nano: Ctrl+O, Enter
# Выйти из nano: Ctrl+X

# Открыть файл в vim (продвинутый редактор)
vim file.txt

# Режим вставки в vim: i
# Сохранить и выйти в vim: Esc, затем :wq
# Выйти без сохранения: Esc, затем :q!
```

### Права доступа

```bash
# Сделать файл исполняемым
chmod +x script.sh

# Права только на чтение
chmod 444 file.txt

# Полные права для владельца
chmod 755 script.sh

# Посмотреть права
ls -l file.txt
```

---

## 🛠️ Bash Scripting

### Структура bash скрипта

```bash
#!/bin/bash

# Shebang - указывает какой интерпретатор использовать

# Переменные
NAME="Admin"
DATE=$(date +%Y%m%d)

# Вывод
echo "Hello, $NAME"

# Условия
if [ -f "file.txt" ]; then
    echo "Файл существует"
else
    echo "Файл не найден"
fi

# Циклы
for i in {1..5}; do
    echo "Итерация $i"
done

# Функции
function my_func() {
    echo "Функция вызвана"
}
my_func
```

### Полезные конструкции

```bash
# Получить вывод команды в переменную
RESULT=$(ls -l)
COUNT=$(cat file.txt | wc -l)

# Проверка существования файла
if [ -f "file.txt" ]; then
    echo "Файл существует"
fi

# Проверка существования директории
if [ -d "folder/" ]; then
    echo "Директория существует"
fi

# Дата и время
DATE=$(date '+%Y-%m-%d %H:%M:%S')
TIMESTAMP=$(date +%Y%m%d_%H%M%S)

# ANSI цвета
RED='\033[0;31m'
GREEN='\033[0;32m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

echo -e "${GREEN}Успешно!${NC}"
echo -e "${RED}Ошибка!${NC}"
```

---

## 🔢 Утилиты обработки текста

### wc - Подсчёт строк/слов/символов

```bash
# Подсчитать строки (-l = lines)
cat file.txt | wc -l

# Подсчитать слова (-w = words)
cat file.txt | wc -w

# Подсчитать символы (-c = characters)
cat file.txt | wc -c

# Пример: количество ошибок 422
dclogs backend | grep "422" | wc -l
```

### awk - Обработка колонок

```bash
# Вывести вторую колонку
echo "one two three" | awk '{print $2}'

# Вывести первую и третью колонку
cat file.txt | awk '{print $1, $3}'

# Извлечь HTTP коды (второе поле)
dclogs backend | grep -oE 'HTTP/1\.1" [0-9]{3}' | awk '{print $2}'

# Подсчитать сумму чисел в первой колонке
cat numbers.txt | awk '{sum+=$1} END {print sum}'
```

### sed - Stream editor (замена текста)

```bash
# Заменить "foo" на "bar" (первое вхождение в строке)
cat file.txt | sed 's/foo/bar/'

# Заменить все вхождения (флаг g = global)
cat file.txt | sed 's/foo/bar/g'

# Удалить пустые строки
cat file.txt | sed '/^$/d'

# Удалить строки содержащие "ERROR"
cat file.txt | sed '/ERROR/d'
```

---

## 📊 Практические примеры из проекта

### Анализ логов backend (День 2, Задание 1-7)

```bash
# 1. Подсчитать все HTTP 422 ошибки
dclogs backend | grep "422" | wc -l

# 2. Подсчитать все HTTP 400 ошибки
dclogs backend | grep "400" | wc -l

# 3. Показать первые 5 строк с ошибкой 422
dclogs backend | grep "422" | head -5

# 4. Показать последние 3 строки с ошибкой 400
dclogs backend | grep "400" | tail -3

# 5. Статистика по всем HTTP кодам
dclogs backend | grep -oE 'HTTP/1\.1" [0-9]{3}' | awk '{print $2}' | sort | uniq -c

# 6. Топ-5 IP адресов
dclogs backend | grep -oE '[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+' | sort | uniq -c | sort -rn | head -5

# 7. Создать bug report (автоматический скрипт)
./generate_bug_report.sh
```

### Работа с тестами

```bash
# Запустить нагрузочный тест
python load_test.py

# Посмотреть логи тестов
dclogs tests

# Запустить тесты вручную
docker-compose run tests pytest -v
```

---

## 🎯 Горячие клавиши в терминале

```bash
# Очистить терминал
Ctrl + L
# или команда:
clear

# Остановить выполнение команды
Ctrl + C

# Поиск в истории команд
Ctrl + R

# Автодополнение пути/команды
Tab

# Перейти в начало строки
Ctrl + A

# Перейти в конец строки
Ctrl + E

# Удалить всю строку
Ctrl + U

# Удалить слово перед курсором
Ctrl + W
```

---

## 📁 Навигация по файловой системе

```bash
# Текущая директория
pwd

# Перейти в директорию
cd /path/to/directory

# Вернуться в предыдущую директорию
cd -

# Перейти в домашнюю директорию
cd ~

# Список файлов
ls

# Список с деталями (права, размер, дата)
ls -l

# Показать скрытые файлы
ls -a

# Список с размерами в человекочитаемом формате
ls -lh

# Создать директорию
mkdir my_folder

# Создать вложенные директории
mkdir -p folder/subfolder/subsubfolder

# Удалить файл
rm file.txt

# Удалить директорию рекурсивно
rm -rf folder/

# Копировать файл
cp source.txt destination.txt

# Копировать директорию
cp -r source_folder/ destination_folder/

# Переместить/переименовать файл
mv old_name.txt new_name.txt
```

---

## 💡 Полезные трюки и советы

### Pipeline (конвейер команд)

```bash
# Символ | (pipe) передаёт вывод одной команды в другую
command1 | command2 | command3

# Пример: найти → отсортировать → подсчитать → показать топ-5
dclogs backend | grep "422" | sort | uniq -c | sort -rn | head -5
```

### Комбинирование команд

```bash
# && - выполнить вторую команду ТОЛЬКО если первая успешна
mkdir test && cd test

# || - выполнить вторую команду ТОЛЬКО если первая НЕ успешна
mkdir test || echo "Ошибка создания директории"

# ; - выполнить обе команды последовательно (независимо от результата)
echo "First" ; echo "Second"
```

### Переменные окружения

```bash
# Установить переменную (только для текущей сессии)
export API_URL="http://localhost:8000"

# Использовать переменную
echo $API_URL

# Посмотреть все переменные окружения
env

# Посмотреть конкретную переменную
echo $PATH
```

---

## 🔗 Быстрый доступ (WSL)

```bash
# Перейти в проект
cd /mnt/c/Users/Admin/PycharmProjects/DockerKube

# Добавить алиас для быстрого доступа (в ~/.bashrc)
alias cdproject='cd /mnt/c/Users/Admin/PycharmProjects/DockerKube'

# Применить изменения в .bashrc
source ~/.bashrc

# Теперь можно использовать
cdproject
```

---

## 📖 Дополнительные ресурсы

**Документация в проекте:**
- `LEARNING_PROGRESS.md` - план обучения и прогресс
- `docs/learning/docker-compose-explained.md` - подробный разбор Docker Compose
- `scripts/README.md` - документация по скриптам

**Полезные команды для справки:**
```bash
# Справка по команде
man grep
man docker-compose

# Краткая справка (для большинства команд)
grep --help
docker-compose --help
```

---

## 🚀 Продвинутые команды (День 3)

### date - Форматирование даты и времени

```bash
# Текущая дата в формате YYYYMMDD_HHMMSS
date +%Y%m%d_%H%M%S
# Вывод: 20251223_143052

# Формат YYYY-MM-DD
date +%Y-%m-%d
# Вывод: 2025-12-23

# Полная дата и время
date '+%Y-%m-%d %H:%M:%S'
# Вывод: 2025-12-23 14:30:52

# Unix timestamp (секунды с 1970-01-01)
date +%s
# Вывод: 1703339452

# Название дня недели
date +%A
# Вывод: Saturday

# Использование в переменной (для backup файлов)
TIMESTAMP=$(date +%Y%m%d_%H%M%S)
BACKUP_FILE="backup_${TIMESTAMP}.log"
```

**Полезные форматы date:**
- `%Y` - год (2025)
- `%m` - месяц (01-12)
- `%d` - день (01-31)
- `%H` - час 24-часовой (00-23)
- `%M` - минуты (00-59)
- `%S` - секунды (00-59)
- `%A` - название дня недели
- `%s` - Unix timestamp

### tar - Архивация файлов

```bash
# Создать tar.gz архив
tar -czf archive.tar.gz folder/
# -c = create, -z = gzip, -f = file

# Создать tar.bz2 архив (лучше сжатие)
tar -cjf archive.tar.bz2 folder/
# -j = bzip2

# Создать архив с verbose (показывать процесс)
tar -czvf archive.tar.gz folder/

# Распаковать tar.gz
tar -xzf archive.tar.gz
# -x = extract

# Распаковать в конкретную директорию
tar -xzf archive.tar.gz -C /tmp/

# Просмотреть содержимое без распаковки
tar -tzf archive.tar.gz
# -t = list

# Создать архив нескольких файлов
tar -czf logs_backup.tar.gz *.log

# Исключить файлы при архивации
tar -czf archive.tar.gz folder/ --exclude='*.log'
```

**Расширения архивов:**
- `.tar` - без сжатия
- `.tar.gz` или `.tgz` - gzip (быстро, среднее сжатие)
- `.tar.bz2` или `.tbz2` - bzip2 (медленно, лучше сжатие)
- `.tar.xz` - xz (очень медленно, максимальное сжатие)

### find - Поиск файлов

```bash
# Найти все .log файлы
find . -name "*.log" -type f

# Найти файлы старше 7 дней
find . -type f -mtime +7

# Найти файлы новее 3 дней
find . -type f -mtime -3

# Найти файлы ровно 5 дней назад
find . -type f -mtime 5

# Найти файлы старше 30 дней и удалить
find . -type f -mtime +30 -delete

# Найти файлы старше 7 дней с деталями
find . -type f -mtime +7 -ls

# Найти файлы больше 100MB
find . -type f -size +100M

# Найти и выполнить команду для каждого файла
find . -name "*.log" -exec ls -lh {} \;

# Найти файлы по нескольким паттернам
find . -type f \( -name "*.log" -o -name "*.txt" \)
```

**Временные параметры find:**
- `-mtime N` - файл модифицирован N дней назад
- `-mtime +N` - старше N дней
- `-mtime -N` - новее N дней
- `-atime N` - время последнего доступа
- `-mmin N` - минуты вместо дней

### docker-compose logs (расширенные опции)

```bash
# Логи без ANSI цветовых кодов
docker-compose logs --no-color backend

# Логи с timestamps
docker-compose logs --timestamps backend

# Логи с timestamps без цвета (для сохранения)
docker-compose logs --no-color --timestamps backend > logs.txt

# Логи за последние 2 часа
docker-compose logs --since="2h" backend

# Логи до определённой даты
docker-compose logs --until="2025-12-23" backend

# Комбинация опций
docker-compose logs --no-color --timestamps --tail=100 backend > backup.log
```

### docker inspect - Детальная информация

```bash
# Полная информация о контейнере (JSON)
docker inspect CONTAINER_ID

# Извлечь статус контейнера
docker inspect --format='{{.State.Status}}' CONTAINER_ID

# Извлечь IP адрес
docker inspect --format='{{.NetworkSettings.IPAddress}}' CONTAINER_ID

# Извлечь время запуска
docker inspect --format='{{.State.StartedAt}}' CONTAINER_ID

# Извлечь переменные окружения
docker inspect --format='{{range .Config.Env}}{{println .}}{{end}}' CONTAINER_ID

# Проверить volumes
docker inspect --format='{{json .Mounts}}' CONTAINER_ID
```

**Go template синтаксис:**
- `{{.Path.To.Field}}` - доступ к полю
- `{{range .Array}}{{.}}{{end}}` - цикл по массиву
- `{{json .Field}}` - вывод в JSON

### docker stats - Мониторинг ресурсов

```bash
# Статистика всех контейнеров (обновление в реальном времени)
docker stats

# Один раз (без постоянного обновления)
docker stats --no-stream

# Кастомный формат вывода
docker stats --no-stream --format "table {{.Name}}\t{{.CPUPerc}}\t{{.MemUsage}}"

# Только определённый контейнер
docker stats backend --no-stream

# Вывести только имя и CPU
docker stats --no-stream --format "{{.Name}}: CPU {{.CPUPerc}}"
```

**Доступные поля:**
- `{{.Container}}` - ID контейнера
- `{{.Name}}` - имя контейнера
- `{{.CPUPerc}}` - CPU в процентах
- `{{.MemUsage}}` - использование памяти (123MB / 2GB)
- `{{.MemPerc}}` - память в процентах
- `{{.NetIO}}` - сетевой I/O
- `{{.BlockIO}}` - дисковый I/O

### curl - HTTP проверки

```bash
# HTTP запрос с кодом ответа
curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/
# -s = silent, -o = output to file, -w = write-out format

# HTTP запрос с таймаутом
curl -s -o /dev/null -w "%{http_code}" --max-time 5 http://localhost:8000/

# Полный вывод (статус + время + размер)
curl -w "HTTP: %{http_code}, Time: %{time_total}s, Size: %{size_download}\n" http://localhost:8000/

# Только заголовки (headers)
curl -I http://localhost:8000/

# POST запрос с JSON
curl -X POST -H "Content-Type: application/json" -d '{"key":"value"}' http://localhost:8000/api

# Проверить доступность (0 или 1)
curl --head --silent --fail http://localhost:8000/ > /dev/null && echo "UP" || echo "DOWN"
```

**Полезные опции curl:**
- `-s` (--silent) - тихий режим
- `-o FILE` - сохранить в файл
- `-w FORMAT` - формат вывода
- `-I` - только headers
- `--max-time N` - таймаут в секундах
- `-X METHOD` - HTTP метод (GET, POST, PUT, DELETE)

### command -v - Проверка наличия команды

```bash
# Проверить наличие команды
command -v docker

# Использование в скрипте
if ! command -v docker &> /dev/null; then
    echo "docker не установлен"
    exit 1
fi

# Альтернативные способы
which docker          # Старый способ
type docker           # Bash встроенная команда
hash docker 2>/dev/null  # Проверка hash table
```

### du - Размер файлов и директорий

```bash
# Размер директории
du -sh folder/
# -s = summarize, -h = human-readable

# Размер каждого файла + итог
du -ch folder/*
# -c = total

# Размер всех .log файлов
du -ch *.log | tail -1

# Сортировка по размеру (top 10)
du -h folder/* | sort -hr | head -10

# Размер с глубиной 1 уровень
du -h --max-depth=1 folder/

# Подсчёт через find
find . -name "*.tar.gz" -exec du -ch {} + | tail -1
```

### Bash условия и проверки

```bash
# Проверка существования файла
if [ -f "file.txt" ]; then
    echo "Файл существует"
fi

# Проверка существования директории
if [ -d "folder/" ]; then
    echo "Директория существует"
fi

# Проверка что переменная не пустая
if [ -n "$VAR" ]; then
    echo "Переменная установлена"
fi

# Проверка что переменная пустая
if [ -z "$VAR" ]; then
    echo "Переменная пустая"
fi

# Проверка числа (regex)
if [[ $NUM =~ ^[0-9]+$ ]]; then
    echo "Это число"
fi

# Проверка exit code
command
if [ $? -eq 0 ]; then
    echo "Команда успешна"
fi
```

**Операторы проверки:**
- `-f FILE` - файл существует
- `-d DIR` - директория существует
- `-e PATH` - путь существует (файл или директория)
- `-r FILE` - файл доступен для чтения
- `-w FILE` - файл доступен для записи
- `-x FILE` - файл исполняемый
- `-n STRING` - строка не пустая
- `-z STRING` - строка пустая

---

## 📊 Практические скрипты (День 3)

### cleanup_logs.sh - Очистка старых логов

```bash
# Показать что будет удалено (dry-run по умолчанию)
./scripts/cleanup_logs.sh --days=7

# Реально удалить логи старше 7 дней
./scripts/cleanup_logs.sh --days=7 --force

# Удалить .txt файлы старше 30 дней
./scripts/cleanup_logs.sh --days=30 --pattern='*.txt' --force
```

### monitoring_script.sh - Мониторинг сервисов

```bash
# Полная проверка всех сервисов
./scripts/monitoring_script.sh

# Быстрая проверка (без логов)
./scripts/monitoring_script.sh --quick

# Проверка только backend
./scripts/monitoring_script.sh --service=backend
```

### backup_logs.sh - Бэкап логов

```bash
# Простой бэкап всех сервисов
./scripts/backup_logs.sh

# Бэкап + архивация
./scripts/backup_logs.sh --archive

# Бэкап + архив + удалить старые архивы
./scripts/backup_logs.sh --archive --cleanup-days=7

# Бэкап только backend (последние 1000 строк)
./scripts/backup_logs.sh --service=backend --lines=1000
```

---

**Последнее обновление:** 2025-12-23 (День 3, Неделя 1)
**Изучено команд:** 70+
**Практических примеров:** 35+
**Созданных скриптов:** 3

> 💡 **Совет:** Держи эту шпаргалку открытой во время работы. Со временем команды запомнятся автоматически!
