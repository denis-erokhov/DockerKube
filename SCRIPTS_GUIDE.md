# 🛠️ Шпаргалка: Какой скрипт когда использовать

Быстрое руководство по всем скриптам автоматизации в проекте.

---

## 📊 Таблица выбора скрипта

| Задача | Скрипт | Команда |
|--------|--------|---------|
| **Разовый backup логов** | `backup_logs.sh` | `./scripts/backup_logs.sh --service=all` |
| **Ежедневная ротация (cron)** | `logs_rotation.sh` | `./scripts/logs_rotation.sh --service=all --compress` |
| **Найти медленные запросы** | `find_slow_requests.sh` | `./scripts/find_slow_requests.sh --threshold=1.0` |
| **Анализ ошибок с отчётом** | `error_patterns.sh` | `./scripts/error_patterns.sh --service=backend --report` |
| **Live мониторинг с алертами** | `live_monitor.sh` | `./scripts/live_monitor.sh --service=backend --alert-on=ERROR` |
| **Удалить старые логи** | `cleanup_logs.sh` | `./scripts/cleanup_logs.sh --days=30 --force` |
| **Проверить здоровье сервисов** | `monitoring_script.sh` | `./scripts/monitoring_script.sh` |
| **Создать нагрузку для теста** | `load_test.py` | `python scripts/load_test.py` |

---

## 🎯 Сценарии использования

### 1️⃣ Перед деплоем новой версии

**Что делаем:** Сохраняем текущие логи на случай проблем

```bash
# Создать backup всех логов
./scripts/backup_logs.sh --service=all --archive

# Проверить здоровье перед деплоем
./scripts/monitoring_script.sh

# Результат: logs/backups/backup_YYYYMMDD_HHMMSS.tar.gz
```

---

### 2️⃣ Расследование инцидента в продакшене

**Что делаем:** Анализируем ошибки и находим проблемные запросы

```bash
# Шаг 1: Создать backup текущих логов
./scripts/backup_logs.sh --service=backend

# Шаг 2: Анализ паттернов ошибок
./scripts/error_patterns.sh --service=backend --report
# → logs/analysis/error_reports/backend_errors_YYYYMMDD.html

# Шаг 3: Найти медленные запросы
./scripts/find_slow_requests.sh --threshold=0.5
# → Покажет запросы медленнее 0.5 сек

# Шаг 4: Live мониторинг на случай повторения
./scripts/live_monitor.sh --service=backend --alert-on=ERROR --save-alerts
```

---

### 3️⃣ Настройка автоматического мониторинга

**Что делаем:** Настраиваем cron для ежедневного архивирования и отчётов

```bash
# Открыть crontab
crontab -e

# Добавить задачи:

# Ежедневная ротация логов в 2:00
0 2 * * * cd /path/to/DockerKube && ./scripts/logs_rotation.sh --service=all --compress --max-files=7

# Ежедневный отчёт об ошибках в 3:00
0 3 * * * cd /path/to/DockerKube && ./scripts/error_patterns.sh --service=backend --report

# Еженедельный анализ медленных запросов (воскресенье в 4:00)
0 4 * * 0 cd /path/to/DockerKube && ./scripts/find_slow_requests.sh --threshold=1.0 --output=logs/analysis/slow_requests/weekly_$(date +%Y%m%d).txt

# Очистка старых логов каждый понедельник в 5:00
0 5 * * 1 cd /path/to/DockerKube && ./scripts/cleanup_logs.sh --days=30 --force

# Проверка здоровья каждые 6 часов
0 */6 * * * cd /path/to/DockerKube && ./scripts/monitoring_script.sh
```

---

### 4️⃣ Тестирование производительности

**Что делаем:** Генерируем нагрузку и анализируем результаты

```bash
# Шаг 1: Создать нагрузку
python scripts/load_test.py
# Создаст 100 запросов к API

# Шаг 2: Найти медленные запросы
./scripts/find_slow_requests.sh --threshold=1.0
# Покажет статистику: min, max, avg, p50, p95, p99

# Шаг 3: Проверить ошибки
./scripts/error_patterns.sh --service=backend --report

# Шаг 4: Backup результатов
./scripts/backup_logs.sh --service=backend
```

---

### 5️⃣ Ежедневная проверка (DevOps routine)

**Что делаем:** Быстро проверяем состояние системы

```bash
# Проверка здоровья всех сервисов
./scripts/monitoring_script.sh --quick

# Если нашли проблемы - детальный анализ
./scripts/error_patterns.sh --service=backend --report

# Посмотреть последние алерты
tail -50 logs/alerts/alerts.log

# Проверить размер логов
du -sh logs/*/
```

---

## 📂 Куда скрипты сохраняют результаты

| Скрипт | Результат сохраняется в |
|--------|------------------------|
| `backup_logs.sh` | `logs/backups/` |
| `logs_rotation.sh` | `logs/rotated/` |
| `error_patterns.sh` | `logs/analysis/error_reports/` |
| `find_slow_requests.sh` | Экран (можно `--output=файл`) |
| `live_monitor.sh` | `logs/alerts/alerts.log` |
| `monitoring_script.sh` | Экран |

---

## 🔧 Полезные комбинации

### Комбинация 1: Полный анализ логов

```bash
#!/bin/bash
# analyze_all.sh - комплексный анализ

echo "1. Backup текущих логов..."
./scripts/backup_logs.sh --service=all --archive

echo "2. Анализ ошибок..."
./scripts/error_patterns.sh --service=backend --report

echo "3. Поиск медленных запросов..."
./scripts/find_slow_requests.sh --threshold=1.0

echo "4. Проверка здоровья..."
./scripts/monitoring_script.sh

echo "Готово! Результаты в logs/analysis/"
```

### Комбинация 2: Очистка всего старого

```bash
#!/bin/bash
# cleanup_all.sh - удалить всё старое

echo "1. Удаление старых логов (30+ дней)..."
./scripts/cleanup_logs.sh --days=30 --force

echo "2. Удаление старых backup (60+ дней)..."
./scripts/backup_logs.sh --cleanup-days=60

echo "3. Проверка размера..."
du -sh logs/*/

echo "Готово!"
```

---

## 🆘 Частые вопросы

### Q: Какой скрипт использовать для ежедневного архивирования?

**A:** `logs_rotation.sh` через cron. Он автоматически сжимает и удаляет старые.

```bash
0 2 * * * cd /path/to/DockerKube && ./scripts/logs_rotation.sh --service=all --compress --max-files=7
```

---

### Q: Как найти причину медленной работы API?

**A:** Используй `find_slow_requests.sh` и `error_patterns.sh`:

```bash
# Найти медленные запросы
./scripts/find_slow_requests.sh --threshold=0.5

# Анализ ошибок
./scripts/error_patterns.sh --service=backend --report
```

---

### Q: Как настроить алерты на ошибки?

**A:** Используй `live_monitor.sh`:

```bash
# В фоне с сохранением алертов
nohup ./scripts/live_monitor.sh --service=backend --alert-on=ERROR --save-alerts > /dev/null 2>&1 &

# Проверить алерты
tail -f logs/alerts/alerts.log
```

---

### Q: В чём разница между backups/ и rotated/?

**A:**

| backups/ | rotated/ |
|----------|----------|
| Ручной запуск | Автоматический (cron) |
| Перед важными событиями | Ежедневно |
| Без сжатия (по умолчанию) | Всегда сжатые |
| Хранятся долго | Автоудаление старых |

---

### Q: Как очистить диск от старых логов?

**A:** Используй `cleanup_logs.sh`:

```bash
# Предпросмотр
./scripts/cleanup_logs.sh --days=30 --dry-run

# Реально удалить
./scripts/cleanup_logs.sh --days=30 --force
```

---

## 📚 Дополнительная документация

- **Детали скриптов:** `scripts/README.md`
- **Структура логов:** `logs/README.md`
- **Автоматизация:** `AUTOMATION.md`
- **Команды:** `COMMANDS_CHEATSHEET.md`

---

**Создано:** 2025-12-27
**Цель:** Быстро найти нужный скрипт для любой задачи
